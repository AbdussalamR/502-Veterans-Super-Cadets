<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="display-5 fw-bold text-dark mb-1">
      <i class="bi bi-calendar-event me-2 text-primary"></i>Events
    </h1>
    <p class="text-muted mb-0">View and manage upcoming events</p>
  </div>
  <% if current_user&.admin? %>
    <%= link_to new_event_path, class: "btn btn-primary btn-lg shadow-sm" do %>
      <i class="bi bi-plus-circle me-2"></i>New Event
    <% end %>
  <% end %>
</div>

<!-- Add this near the top of app/views/events/index.html.erb -->
<div class="feed-subscriptions" style="margin-bottom: 20px; padding: 15px; background: #f8f9fa; border-radius: 5px;">
  <h4>Subscribe to Events</h4>
  <%= link_to "Calendar Feed (.ics)", events_path(format: :ics), class: "btn btn-primary", style: "margin-right: 10px;" %>
  <%= link_to "RSS Feed", events_path(format: :rss), class: "btn btn-secondary" %>
  <p style="margin-top: 10px; font-size: 0.9em; color: #666;">
    Subscribe to stay updated on upcoming events
  </p>
</div>

<!-- Your existing upcoming/past events sections below -->

<% if @upcoming_events.empty? && @past_events.empty? %>
  <!-- Empty State -->
  <div class="row justify-content-center">
    <div class="col-lg-6 col-md-8">
      <div class="card border-0 shadow-sm text-center py-5">
        <div class="card-body">
          <div class="text-primary mb-4">
            <i class="bi bi-calendar-x display-1"></i>
          </div>
          <h4 class="card-title text-dark mb-3">No Events Yet</h4>
          <p class="card-text text-muted mb-4">
            There are no events scheduled at the moment. 
            <% if current_user&.admin? %>
              Get started by creating your first event.
            <% else %>
              Check back later for new events.
            <% end %>
          </p>
          <% if current_user&.admin? %>
            <%= link_to new_event_path, class: "btn btn-primary" do %>
              <i class="bi bi-plus-circle me-2"></i>Create First Event
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  <!-- Upcoming Events -->
  <% unless @upcoming_events.empty? %>
    <h2 class="fw-bold text-dark mb-3">Upcoming Events</h2>
    <div class="row g-4 mb-5">
      <% @upcoming_events.each do |event| %>
        <%= render partial: "event_card", locals: { event: event, current_user: current_user } %>
      <% end %>
    </div>
  <% end %>

  <!-- Past Events -->
  <% unless @past_events.empty? %>
    <h2 class="fw-bold text-dark mb-3">Past Events</h2>
    <div class="row g-4">
      <% @past_events.each do |event| %>
        <%= render partial: "event_card", locals: { event: event, current_user: current_user } %>
      <% end %>
    </div>
  <% end %>

<% end %>

<!-- Custom Styles for Hover Effects -->
<style>
  .hover-lift {
    transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
    position: relative;
    z-index: 1;
  }
  
  .hover-lift:hover {
    transform: translateY(-2px);
    box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
    z-index: 10;
  }
  
  .card-header {
    border-top-left-radius: calc(0.375rem - 1px);
    border-top-right-radius: calc(0.375rem - 1px);
  }
</style>
